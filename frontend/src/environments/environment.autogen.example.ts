/* eslint-disable */
// Example shape only. The real `environment.autogen.ts` is generated (gitignored) by:
//   npm run clerk:env
// and also by postinstall / prestart / prebuild.
export const clerkPublishableKey = '';
